{
    "name": "Smart Parking Python Env",
    "image": "mcr.microsoft.com/devcontainers/python:3.9",
    "postCreateCommand": "sudo apt-get update && sudo apt-get install libgl1 -y && pip install -r requirements.txt"
}